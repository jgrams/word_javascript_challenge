"var allLetters = new RegExp('[a-zA-Z]+');var anyWhiteSpace = new RegExp('\\s');function LongestWord(sen) {   var longest_word = null;  var longest_word_length = 0;  var words = sen.split(anyWhiteSpace);    for (i = 0; i < words.length; i++) {    var word = words[i].match(allLetters)[0];     if (longest_word_length < word.length) {      longest_word = word;      longest_word_length = word.length;    }  }  return longest_word;}" 
